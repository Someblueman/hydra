{
  "schema_version": 1,
  "selected_at_utc": "2026-09-10T13:49:13.715273+00:00",
  "selected": "parallel",
  "comparison": "draft-compare.json",
  "reason": "Functional and sanitizer checks consume the same sealed patch and do not depend on each other. Remove the artificial edge while retaining independent fresh checks. Overlapping uncalibrated ranges and unknown costs leave numeric preference unresolved.",
  "rejected": "serial has no additional correctness guarantee and serializes independent work; retain as matched fixed-graph scheduling control.",
  "implementation_authoring": "Codex root plus the original authorized H2 worker; outside the execution graph.",
  "draft_artifact": "Placeholder only, explicitly unimplemented; comparison is pre-implementation structural planning, not accepted execution. Final input and source hashes require recompilation/admission.",
  "planning_corrections": [
    "Initial envelope exceeded existing artifact-byte maximum. Revised to a patch deliverable within the existing 512KiB file limit; no runtime limits widened.",
    "Aggregate declared exec time requires3600s bound; original1800s would not cover two1500s checks plus compose. Bound is below task deadline and not an estimate."
  ],
  "execution_revision": {
    "at_utc": "2026-09-10T14:11:54.578601+00:00",
    "reason": "The existing-head admission gate refused reuse of prior execution identities. Use distinct head names for each schedule; preserve the same work, source, inputs, checks and budgets. This is execution isolation, not a decomposition change."
  }
}
