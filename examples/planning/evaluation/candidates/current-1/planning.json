{
  "candidate": "current-1",
  "contract_set": "9e-visible-contracts-20260910-v1",
  "grouping": [
    {"group": "feature", "contracts": ["normalize_records", "reject_duplicate_config", "compose_two_members"], "reason": "small deterministic byte and text transforms with independent dispatch"},
    {"group": "research", "contracts": ["schedule_metrics", "threshold_claim", "preserve_unknowns"], "reason": "finite calculations and explicit unknown propagation"},
    {"group": "manifest", "contracts": ["exact_members", "hash_bound_members", "selected_skipped"], "reason": "ordered membership, byte hashing, and explicit record coverage"}
  ],
  "dependencies": [
    "Each request is self-contained; no contract depends on another contract's output.",
    "Manifest hashing uses SHA-256 over supplied member bytes only.",
    "All JSON serialization is compact and deterministic; ordering follows each contract."
  ],
  "decomposition": {"mode": "fixed dispatch", "parallelism": 1, "reason": "one bounded request is solved without shared mutable state"},
  "host_placement": {"mode": "local", "reason": "the packet forbids network access and requires only Python standard library"},
  "time_ranges_seconds": {"implementation": [1, 10], "validation": [1, 10], "transfer": [0, 1]},
  "effort": {"implementation": "small", "validation": "small", "transfer_obligations": "none beyond supplied JSON input"},
  "validation": ["Run each supplied training example through solve and compare exact artifact values.", "Run syntax compilation and bounded CLI smoke checks."],
  "limitations": ["No global optimality claim.", "No probabilities or confidence estimates are generated.", "No values are inferred for literal unknown.", "Process exit is not treated as semantic acceptance; artifacts must be compared."],
  "training_observations": ["The schedule example requires stable SJF ties and six-place mean rounding.", "The hash example is intentionally a mismatch and must remain fail.", "Selection false values require explicit skipped records."],
  "actual_start_utc": null,
  "actual_end_utc": null,
  "elapsed_seconds": null,
  "measurement_state": "not_recorded",
  "measurement_note": "Generation completed before exact wall-clock timing was recorded; the duration cannot be reconstructed."
}
